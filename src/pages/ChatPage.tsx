import { Box, Typography } from '@mui/material';

export default function ChatPage() {
  return (
    <Box p={4}>
      <Typography variant="h4">Chat</Typography>
      <Typography variant="body1">Tela em construção</Typography>
    </Box>
  );
}